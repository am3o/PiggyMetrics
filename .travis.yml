language: go

go:
  - release

before_install:
  - go get -u github.com/golang/dep/cmd/dep
  - dep ensure

script:
  - go test -cover -race -v $(go list ./... | grep -v /vendor/ | grep -v /integration | tr "\n" " ")
  - go build -o bin/account cmd/account/account_server.go